.page-header
  %h1
    = @project.title
    %small= @project.client
    .pull-right
      %small= link_to "Edit |", edit_project_path(@project)
      %small= link_to "Destroy", project_path(@project), method: :delete
