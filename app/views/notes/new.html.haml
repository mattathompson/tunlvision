%br
%br
%br
%br

.form-column
  %h1 Create a new note
  =form_for([@project, @note]) do |f|
    = f.label :title
    = f.text_field :title

    = f.label :content
    = f.text_area :content, rows: 8

    = f.submit "Post Note"
